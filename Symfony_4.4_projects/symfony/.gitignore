# ici on est dans symfony/.gitignore

#  rules:
# bin	ignore all files and directories (and content) named bin 
# bin/	ignore all directories named bin and content
# bin/*	ignore content of all directories named bin, but not the bin directories themself 
# If there is a separator at the end of the pattern then the pattern will only match directories, 
# otherwise the pattern can match both files and directories.
/.idea
.idea/misc.xml
/.env
/.env.local
/.env.local.php
/.env.*.local
/public/bundles/
var/cache/*
/vendor/
###< symfony/framework-bundle ###

###> symfony/phpunit-bridge ###
.phpunit
.phpunit.result.cache
/phpunit.xml
###< symfony/phpunit-bridge ###

###> symfony/web-server-bundle ###
/.web-server-pid
###< symfony/web-server-bundle ###

###> symfony/webpack-encore-bundle ###
/node_modules/
/public/build/
npm-debug.log
yarn-error.log
###< symfony/webpack-encore-bundle ###
*.exe

